// import AgentCard from "./AgentCard";

// export default function AgentFlow({ agents }) {
//   return (
//     <div className="flex flex-col items-center mt-6">

//       {agents?.planner && (
//         <AgentCard title="Planner" emoji="🤖" content={agents.planner} />
//       )}

//       {agents?.executor && (
//         <AgentCard title="Executor" emoji="⚙️" content={agents.executor} />
//       )}

//       {agents?.critic && (
//         <AgentCard title="Critic" emoji="💀" content={agents.critic} />
//       )}

//       {agents?.final && (
//         <AgentCard title="Final Decision" emoji="🧠" content={agents.final} />
//       )}

//     </div>
//   );
// }